# Build output
/agent
/agent.exe
/flynn
/flynn.exe
/dist/
*.exe
*.out

# CI artifacts
coverage.out

# dev/badges output. It is published to the `badges` branch by CI, never to main.
badges/
results.sarif

# rapid property-test failure reproductions (regenerated on failure, not committed).
# The leading **/ matters: a pattern with a slash in it is anchored to this file's
# directory, so a bare testdata/rapid/ only ever matched one at the repo root.
**/testdata/rapid/

# Local config / secrets
.env
*.local

# Local tooling config
CLAUDE.md
.claude/





# Editor / OS
.DS_Store
.idea/
.vscode/
